# Optional — enables AI-assisted dedup (semantic near-duplicate matching),
# per-finding "why this matters" summaries, and methodology deck drafting
# (ai/client.py). Without it, those steps skip cleanly (logged, not an error);
# deterministic + fuzzy dedup and scoring all work fine without this.
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash   # override if you want a different model

# Optional: raises NVD rate limit from 5 req/30s to 50 req/30s (enrich/nvd.py)
NVD_API_KEY=

# Optional until the ticketing phase — pipeline no-ops cleanly without these
GITHUB_TOKEN=
GITHUB_REPO=Vishal-V2/Threat-X
